<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ngangavictor.grocerystore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".categories.account.AccountActivity"></activity>
        <activity
            android:name=".categories.CategoriesActivity"
            android:label="@string/title_activity_categories"
            android:theme="@style/CategoryTheme" />
        <activity android:name=".reset.ResetPasswordActivity" />
        <activity
            android:name=".login.LoginActivity"
            android:theme="@style/RegisterTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/RegisterTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>